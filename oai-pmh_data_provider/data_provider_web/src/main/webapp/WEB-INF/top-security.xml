<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns:security="http://www.springframework.org/schema/security"
    xsi:schemaLocation="http://www.springframework.org/schema/beans
    http://www.springframework.org/schema/beans/spring-beans.xsd
    http://www.springframework.org/schema/security
    http://www.springframework.org/schema/security/spring-security.xsd"
    default-autowire="no">

	<!-- ======================== SPRING SECURITY 5 CONFIGURATION ======================= -->
	<!-- Migrated from Acegi Security to Spring Security 5.8.15 -->
	<!-- NOTE: This file is currently disabled in web.xml -->

	<!-- HTTP Security Configuration -->
	<security:http auto-config="false" use-expressions="false">
		<!-- URL-based authorization rules (most specific first) -->
		<security:intercept-url pattern="/submit.html" access="User,Admin,Associate Editor"/>
		<security:intercept-url pattern="/user/**" access="User,Admin,Associate Editor"/>
		<security:intercept-url pattern="/admin/**" access="Admin,Associate Editor"/>

		<!-- Form-based authentication -->
		<security:form-login
			login-page="/login.jsp"
			login-processing-url="/j_security_check"
			username-parameter="j_username"
			password-parameter="j_password"
			default-target-url="/user/processUser.html"
			always-use-default-target="true"
			authentication-failure-url="/login.jsp?error=true"/>

		<!-- Logout configuration -->
		<security:logout logout-url="/logout" logout-success-url="/login.jsp"/>

		<!-- Session management with fixation protection -->
		<security:session-management session-fixation-protection="newSession"/>

		<!-- Disable CSRF for backward compatibility (can be enabled later) -->
		<security:csrf disabled="true"/>
	</security:http>

	<!-- Authentication Manager Configuration -->
	<security:authentication-manager>
		<security:authentication-provider user-service-ref="userService">
			<security:password-encoder ref="passwordEncoder"/>
		</security:authentication-provider>
	</security:authentication-manager>

	<!-- Password Encoder: DelegatingPasswordEncoder for backward compatibility -->
	<bean id="passwordEncoder" class="org.cipres.treebase.security.DelegatingPasswordEncoder"/>

	<!-- Role Voter with empty prefix to preserve existing role names (Admin, User, Associate Editor) -->
	<bean id="roleVoter" class="org.springframework.security.access.vote.RoleVoter">
		<property name="rolePrefix" value=""/>
	</bean>

	<!-- Access Decision Manager -->
	<bean id="accessDecisionManager"
	      class="org.springframework.security.access.vote.AffirmativeBased">
		<constructor-arg>
			<list>
				<ref bean="roleVoter"/>
			</list>
		</constructor-arg>
		<property name="allowIfAllAbstainDecisions" value="false"/>
	</bean>

	<!-- Override default access decision manager -->
	<bean id="webExpressionVoter"
	      class="org.springframework.security.web.access.expression.WebExpressionVoter">
		<property name="expressionHandler">
			<bean class="org.springframework.security.web.access.expression.DefaultWebSecurityExpressionHandler"/>
		</property>
	</bean>

</beans>
